import { NeptuneModelingResultSigFigPipe } from './neptune-modeling-result-sig-fig.pipe';

describe('NeptuneModelingResultSigFigPipe', () => {
  it('create an instance', () => {
    const pipe = new NeptuneModelingResultSigFigPipe();
    expect(pipe).toBeTruthy();
  });
});
