apiVersion: v1
kind: Service
metadata:
  name: {{ include "nereid-flower.fullname" . }}
  labels:
    {{- include "nereid-flower.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nereid-flower.selectorLabels" . | nindent 4 }}
