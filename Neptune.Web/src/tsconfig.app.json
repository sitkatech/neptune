{
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "outDir": "../out-tsc/app",
        "types": ["leaflet"]
    },
    "files": ["main.ts"],
    "include": ["src/**/*.d.ts"],
    "exclude": ["**/*.stories.*"]
}
