version: '3.4'

services:
  neptune.webmvc:
    image: neptune/webmvc
    build:
      context: ..
      dockerfile: Neptune.Web/Dockerfile

  neptune.api:
    image: neptune/api
    build:
      context: ..
      dockerfile: Neptune.API/Dockerfile
